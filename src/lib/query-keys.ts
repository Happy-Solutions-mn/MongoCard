/**
 * TanStack Query keys — add when @tanstack/react-query is wired in.
 * Example: export const gameKeys = { all: ["game"] as const };
 */
export const queryKeys = {
  root: ["mongocard"] as const,
} as const;
